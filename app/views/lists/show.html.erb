<h1> <%= @list.name %></h1>
<ul>
<% @list.bookmarks.each do |bookmark| %>
  <li><%= bookmark.comment %> -  <%= bookmark.movie.title %> - <%= link_to "Delete", bookmark_path(bookmark), method: :delete  %></li>
<% end %>
</ul>
<%= link_to "Add a bookmark", new_list_bookmark_path(@list)%>
